1. do measurementでQで値を返すのではなく、それぞれのPadオブジェクトに数字を入れてるので、
SmartStockerの方ではそのPadオブジェクトから計量値を呼ぶ方がいいのか。
Done

2. Thersholdと比較するのは計量値を1周させてからにするのですが、
モーションとかほかがある限り正しいカウントがでないので、その処理を
どうしようかな…たとえば１０周してからモニターする、とか。

3. タブが大量になった際のスクロールの方法-> いちおうSmartStockの数が７以上でスクロールバーひょうじさせた

4.imageをクリックしたら元の画像ファイルを表示する

